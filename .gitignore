# Pipeline runtime artifacts (regenerated every run)
.pipeline/status.json
.pipeline/events.jsonl
.pipeline/logs/
.pipeline/vague_request.txt
.pipeline/specs.md
.pipeline/changes.md
.pipeline/checker_report.md
.pipeline/test_suite.md
.pipeline/review_report.md
.pipeline/test_history.json
.pipeline/.lock
.pipeline/ui-server.pid
.pipeline/ui-server.out
.pipeline/orchestrator.out
.pipeline/ui.url
.pipeline/stage-handoff.json
.pipeline/followups/
.pipeline/diff.patch
.pipeline/runs/
# Update-check cache — ephemeral. .pipeline/install.json IS committed: it pins
# the installed version and is what makes a safe update possible.
.pipeline/update-check.json

# Sandbox worktree
.pipeline_sandbox/

node_modules/
.DS_Store

# Eval run outputs (real-agent runs, machine-specific)
evals/results/

# --- v2 runtime state -------------------------------------------------------
# Artifacts and run state are per-machine; intent and durable records are not.
.pipeline/design.md
.pipeline/handoff.md
.pipeline/reporter.md
.pipeline/review_correctness.md
.pipeline/review_security.md
.pipeline/review_architecture.md
.pipeline/reports/
.pipeline/runs/
.pipeline/worktrees/
.pipeline/skills-cache/
.pipeline/control/snapshot.json
.pipeline/control/attention.jsonl
.pipeline/control/briefs/
.pipeline/control/reports/
.pipeline/control/supervisor.log
.pipeline/control/supervisor.out
.pipeline/control/supervisor.pid
.pipeline/control/.lock
.pipeline/control/paused
.pipeline/control/hook-state.json

# Deliberately tracked, so a roadmap and its record travel with the repo:
#   .pipeline/roadmap.md, .pipeline/control/roadmap.json,
#   .pipeline/control/decisions.jsonl, .pipeline/control/notes/,
#   .pipeline/skills/*.sha256
