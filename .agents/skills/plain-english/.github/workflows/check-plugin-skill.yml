name: Check plugin skill

on:
  push:
  pull_request:

jobs:
  check-skill-copy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check two-skill plugin bundle
        run: bash scripts/check-plugin-bundle.sh
